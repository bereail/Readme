// app/lecturas/editar.tsx
// Pantalla de edición como ruta separada (podrías usar también /lecturas/[id]/editar si preferís anidar).

import React from 'react';
import EditarLecturaScreen from '../screens/EditarLecturaScreen';

export default function EditarLecturaRoute() {
  return <EditarLecturaScreen />;
}
