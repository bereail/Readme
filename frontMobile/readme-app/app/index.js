// index.js
import { registerRootComponent } from 'expo';
import App from '../app.js';

// Esto se encarga de registrar el componente raíz
// y hacerlo compatible con Expo o React Native CLI
registerRootComponent(App);
